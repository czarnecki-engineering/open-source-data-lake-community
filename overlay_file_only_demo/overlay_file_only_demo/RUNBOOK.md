# File Only Demo Runbook

1. Install the additive payload into a compatible repo root.
2. Start the normal base stack with `./start-compose.sh`.
3. Open the Solutions page.
4. Confirm that `File Only Demo` is listed.

This example is intentionally file-only:

- no `--overlay` argument
- no overlay compose YAML
- no overlay wrapper scripts
