# Task 1 Discovery Report
- Repo: open-source-data-lake-community
- Mode: Read-only discovery
- Generated: 2026-04-16 20:07:25 AEST

## A. Confirmed Facts

- The repo defines a local Docker Compose stack with `minio`, `minio-init`, `airflow`, `jupyter`, and `php` services; named volumes are `minio-data` and `airflow-db`. Evidence: `docker-compose.yaml:3-127`.
- MinIO bucket creation is repo-defined and automatic: `minio-init` runs `mc mb --ignore-existing` for `conformed`, `raw`, and `curated`. Evidence: `docker-compose.yaml:23-39`.
- Airflow is a single container running both `airflow webserver` and `airflow scheduler`, using `SequentialExecutor` and SQLite at `/opt/airflow/airflow.db`. Evidence: `docker-compose.yaml:53-96`.
- Airflow mounts `./dags`, `./logs`, `./plugins`, and read-only `./config`; Jupyter mounts `./notebooks`; PHP mounts `./php`. Evidence: `docker-compose.yaml:78-83,108-119`.
- Default repo-documented local access URLs are Airflow `http://localhost:8080`, MinIO Console `http://localhost:9001`, MinIO API `http://localhost:9000`, Jupyter `http://localhost:8888`, and PHP `http://localhost:8088`. Evidence: `README.md:95-99`, `start-compose.sh:44-52`, `php/index.php:5-15`.
- The ASX daily DAG requires `config/asx200_tickers.csv`; missing it is expected to fail the DAG and does not by itself mean platform failure. Evidence: `README.md:81-92`, `config/README.md:3-19`, `dags/asx200_ohlcv_daily_to_raw.py:47-85`.
- Repo docs define platform health as services starting successfully, Airflow UI being accessible, and heartbeat DAGs running. Evidence: `README.md:88-91,105-113`, `RUNBOOK.md:321-339`, `config/README.md:13-16`.

## B. Service Login Inventory

| Service | Evidence | Access URL / port | Auth type | Default credentials / token | Login success vs page load required for validation |
| --- | --- | --- | --- | --- | --- |
| Airflow UI | `docker-compose.yaml:76-96`; `README.md:95`; `php/index.php:6` | `http://localhost:8080` / host port `8080` | username/password | username `minioadmin`, password `minioadmin` via Airflow user creation defaults. Evidence: `docker-compose.yaml:85-89`, `README.md:95` | Page load / accessibility is explicitly part of platform health; login success is NOT VERIFIED. Evidence: `README.md:88-91`, `RUNBOOK.md:321-324` |
| MinIO Console | `docker-compose.yaml:8-15`; `README.md:96`; `php/index.php:8` | `http://localhost:9001` / host port `9001` | username/password | username `minioadmin`, password `minioadmin`. Evidence: `docker-compose.yaml:11-13`, `README.md:96` | NOT VERIFIED |
| MinIO S3 API | `docker-compose.yaml:8-15`; `README.md:97`; `php/index.php:9` | `http://localhost:9000` / host port `9000` | username/password | access key `minioadmin`, secret key `minioadmin`. Evidence: `docker-compose.yaml:11-13,62-64` | NOT VERIFIED |
| Jupyter | `docker-compose.yaml:104-109`; `README.md:98`; `php/index.php:7` | `http://localhost:8888` / host port `8888` | token | token `jupyter`. Evidence: `docker-compose.yaml:106-107`, `README.md:98` | NOT VERIFIED |
| PHP service index | `docker-compose.yaml:115-123`; `README.md:99`; `php/index.php:10` | `http://localhost:8088` / host port `8088` | none | none | NOT VERIFIED |

## C. Stateful Surface Inventory

| Component | Read / write direction | Persistence location | How state is created | How state is consumed | Reset behaviour | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| Docker volume `minio-data` | RW by `minio` | Named Docker volume mounted at `/data` | MinIO writes bucket/object state to `/data` | MinIO serves objects to DAGs, notebooks, and console/API clients | Preserved by `./stop-compose.sh`; removed by `./stop-compose.sh --volumes` | `docker-compose.yaml:14-15,125-127`; `README.md:58-79,119-128`; `RUNBOOK.md:126-185`; `stop-compose.sh:43-49` |
| Docker volume `airflow-db` | RW by `airflow` | Named Docker volume mounted at `/opt/airflow` | `airflow db migrate` and Airflow runtime create SQLite metadata in `/opt/airflow/airflow.db` | Airflow uses it for metadata, user records, scheduler/web state | Preserved by `./stop-compose.sh`; removed by `./stop-compose.sh --volumes` | `docker-compose.yaml:56,83,85-89,125-127`; `README.md:59-79,120-128`; `RUNBOOK.md:128-185`; `stop-compose.sh:43-49` |
| MinIO bucket `raw` | RW | Inside `minio-data` volume | `minio-init` creates bucket; heartbeat and ASX DAGs write objects; backfill writes control/audit state here | Downstream DAGs and notebooks read from it | Deleted only when `minio-data` is removed | `docker-compose.yaml:32-37`; `dags/heartbeat_1m_to_raw.py:19-23`; `dags/asx200_ohlcv_daily_to_raw.py:93-171`; `dags/asx200_ohlcv_backfill_to_raw.py:40,47-48,149-181,184-334`; `RUNBOOK.md:172-173` |
| MinIO bucket `conformed` | RW | Inside `minio-data` volume | `minio-init` creates bucket; heartbeat raw->conformed DAG copies heartbeat objects; ASX raw->conformed DAG writes parquet | Curated snapshot DAG and heartbeat conformed->curated DAG read from it | Deleted only when `minio-data` is removed | `docker-compose.yaml:32-37`; `dags/heartbeat_1m_copy_raw_to_conformed.py:10-35`; `dags/asx200_ohlcv_raw_to_conformed_parquet.py:37-128`; `RUNBOOK.md:172-173` |
| MinIO bucket `curated` | RW | Inside `minio-data` volume | `minio-init` creates bucket; heartbeat conformed->curated DAG copies heartbeat objects; curated snapshot DAG writes `snapshot.parquet` | Notebooks default to reading curated parquet data | Deleted only when `minio-data` is removed | `docker-compose.yaml:32-37`; `dags/heartbeat_1m_copy_conformed_to_curated.py:10-35`; `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:39-42,87-170`; `notebooks/01_asx_eda.ipynb:108-109`; `notebooks/02_asx_preprocessing.ipynb:70-71`; `notebooks/read_ohlcv_daily.ipynb:356-360`; `RUNBOOK.md:172-173` |
| `./config` bind mount | Host -> Airflow read-only | Host repo path `./config`, mounted at `/opt/airflow/config:ro` | Operator manually creates `config/asx200_tickers.csv` locally | Daily and backfill ASX DAGs read ticker config from `/opt/airflow/config/asx200_tickers.csv` | Retained by both stop modes | `docker-compose.yaml:82`; `README.md:21,78,82-92`; `config/README.md:3-19`; `dags/asx200_ohlcv_daily_to_raw.py:24-27,47-85`; `dags/asx200_ohlcv_backfill_to_raw.py:41-44,75-108`; `RUNBOOK.md:154-157,177` |
| `./dags` bind mount | Host -> Airflow read/write mount, repo source in practice | Host repo path `./dags`, mounted at `/opt/airflow/dags` | DAG source files are present in repo | Airflow loads DAG definitions from mounted directory | Retained by both stop modes | `docker-compose.yaml:79`; `README.md:61,79,123,133`; `RUNBOOK.md:130,156,176` |
| `./logs` bind mount | Airflow writes host files | Host repo path `./logs`, mounted at `/opt/airflow/logs` | Airflow runtime writes logs | Operator / Airflow UI can consume logs; exact UI path NOT VERIFIED | Retained by both stop modes; RUNBOOK clean teardown says remove for a true clean run | `docker-compose.yaml:80`; `RUNBOOK.md:102,178,241-257`; `README.md:62,79,123` |
| `./plugins` bind mount | Host <-> Airflow | Host repo path `./plugins`, mounted at `/opt/airflow/plugins` | Repo or operator can place plugin files there | Airflow can load plugin files from the mount; actual plugin use in repo is NOT VERIFIED | Retained by both stop modes | `docker-compose.yaml:81`; `RUNBOOK.md:130,179`; `README.md:62,79,123`; `docs/reference/IMPLEMENTED_CAPABILITIES.md:25` |
| `./notebooks` bind mount | Jupyter reads and writes host files | Host repo path `./notebooks`, mounted at `/home/jovyan/work` | Notebook edits, outputs, and any relative-file writes from Jupyter persist there | Jupyter reopens notebooks and local output files from the bind mount | Retained by both stop modes | `docker-compose.yaml:108-109`; `README.md:61,79,121`; `RUNBOOK.md:101,174,205,263,384`; `notebooks/02_asx_preprocessing.ipynb:340-342` |
| Notebook output file `asx_ohlcv_preprocessed.parquet` | Notebook writes local file | Relative path in Jupyter working directory; with current mount this resolves under `./notebooks` unless env overrides it | `02_asx_preprocessing.ipynb` calls `df.to_parquet(OUT_PATH, index=False)` with default `OUT_PATH="asx_ohlcv_preprocessed.parquet"` | Later notebook or operator can read the file; downstream repo consumption is NOT VERIFIED | No repo-defined automatic reset; retained unless manually deleted | `docker-compose.yaml:108-109`; `notebooks/02_asx_preprocessing.ipynb:340-342` |
| Notebook runtime dirs `eda_output` and `.ipynb_checkpoints` | Notebook / Jupyter writes host files | Host-side runtime directories | RUNBOOK states `eda_output/` is runtime and notebook-created; `.ipynb_checkpoints` cleanup is also documented | Notebook/Jupyter consume them as notebook artefacts; exact producing notebook is NOT VERIFIED | RUNBOOK clean teardown recommends deleting them manually | `RUNBOOK.md:99-103,205,241-257` |
| `./php` bind mount | Host <-> PHP container | Host repo path `./php`, mounted at `/app/public` | Repo provides PHP pages; live edits persist on host | FrankenPHP serves files from the mount | Retained by both stop modes | `docker-compose.yaml:117-123`; `README.md:61,79,122,136`; `RUNBOOK.md:130,156,175` |
| Heartbeat raw objects | DAG writes; downstream DAGs/notebooks read | `raw/heartbeat/airflow_time_YYYYMMDD_HHMMSS.txt` in MinIO | `heartbeat_1m_to_raw` writes timestamp text files every minute | Raw->conformed heartbeat DAG copies them; `read_heartbeat.ipynb` lists and reads them from `raw` | Removed only when MinIO state is reset | `dags/heartbeat_1m_to_raw.py:19-23,26-37`; `notebooks/read_heartbeat.ipynb:53-65,93-116` |
| Heartbeat promoted objects | DAG copy writes | Same object keys under `conformed/heartbeat/` and `curated/heartbeat/` | Copy DAGs copy missing keys only | Used as platform health signal per docs | Removed only when MinIO state is reset | `dags/heartbeat_1m_copy_raw_to_conformed.py:16-35`; `dags/heartbeat_1m_copy_conformed_to_curated.py:16-35`; `README.md:105-113`; `RUNBOOK.md:333-339` |
| ASX raw OHLCV objects | Daily/backfill DAGs write; raw->conformed reads | `raw/tabular/market_ohlcv_daily/exchange=ASX/trade_date=YYYY-MM-DD/ticker=TICKER.csv` | ASX daily and backfill DAGs write one CSV per `(ticker, trade_date)` | Raw->conformed DAG reads CSVs under the same prefix | Removed only when MinIO state is reset | `dags/asx200_ohlcv_daily_to_raw.py:93-171`; `dags/asx200_ohlcv_backfill_to_raw.py:62-67,184-307`; `dags/asx200_ohlcv_raw_to_conformed_parquet.py:37-45,84-105` |
| ASX conformed parquet objects | Raw->conformed DAG writes; curated snapshot reads; notebooks may read curated only by default | `conformed/tabular/market_ohlcv_daily/exchange=ASX/.../*.parquet` | Conversion DAG writes parquet and adds `ingest_ts`, `ingest_batch_id`, `source_object_key`, `row_hash` | Curated snapshot DAG reads parquet objects for each trade date | Append-only; skipped if target key already exists; removed only when MinIO state is reset | `dags/asx200_ohlcv_raw_to_conformed_parquet.py:41-45,84-128`; `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:87-128` |
| ASX curated snapshots | Curated snapshot DAG writes; notebooks read | `curated/tabular/market_ohlcv_daily/exchange=ASX/trade_date=YYYY-MM-DD/snapshot.parquet` | Curated DAG dedupes conformed rows and writes per-date `snapshot.parquet` | `01_asx_eda.ipynb`, `02_asx_preprocessing.ipynb`, and `read_ohlcv_daily.ipynb` default to curated parquet prefix | Snapshot overwrite is allowed when conformed data is newer; removed only when MinIO state is reset | `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:39-42,87-170`; `notebooks/01_asx_eda.ipynb:108-109`; `notebooks/02_asx_preprocessing.ipynb:70-71`; `notebooks/read_ohlcv_daily.ipynb:356-360` |
| Backfill resume state | Backfill DAG reads and writes | `raw/tabular/market_ohlcv_daily/control/asx200_backfill/state.json` | `_load_state` initializes default state if absent; `_save_state` writes state after success/error | Backfill DAG uses it to skip tickers already marked `complete` | Removed only when MinIO state is reset; no finer-grained repo reset documented | `dags/asx200_ohlcv_backfill_to_raw.py:47,149-164,190-214,309-334` |
| Backfill audit errors | Backfill DAG reads and writes | `raw/tabular/market_ohlcv_daily/audit/asx200_backfill/run_date=YYYY-MM-DD/errors.json` | `_append_audit_error` appends per-ticker failures | Backfill DAG uses it as audit trail; any other consumer is NOT VERIFIED | Removed only when MinIO state is reset; no finer-grained repo reset documented | `dags/asx200_ohlcv_backfill_to_raw.py:48,167-181,316-334` |

## D. DAG Validation Inventory

| File path | `dag_id` | Purpose from source | Trigger mode | Exact schedule from code | Required inputs / preconditions | Outputs / state changes | Validation implications |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `dags/heartbeat_1m_to_raw.py` | `heartbeat_1m_to_raw` | Writes current Melbourne time to a raw bucket text object. Evidence: `dags/heartbeat_1m_to_raw.py:10-23` | scheduled | `* * * * *` | Airflow running; S3 endpoint and credentials available via env defaults or overrides. Evidence: `dags/heartbeat_1m_to_raw.py:11-17,26-37`; `docker-compose.yaml:62-65` | Writes `raw/heartbeat/airflow_time_YYYYMMDD_HHMMSS.txt` | Validation can check for recurring raw heartbeat object creation; this is directly tied to documented platform health. Evidence: `README.md:105-113`; `RUNBOOK.md:333-339` |
| `dags/heartbeat_1m_copy_raw_to_conformed.py` | `heartbeat_1m_copy_raw_to_conformed` | Copies missing `heartbeat/` objects from `raw` to `conformed`. Evidence: `dags/heartbeat_1m_copy_raw_to_conformed.py:16-35` | scheduled | `*/1 * * * *` | Raw heartbeat objects must exist; MinIO reachable at `http://minio:9000`. Explicit credentials are NOT VERIFIED in this DAG because boto3 client omits them. Evidence: `dags/heartbeat_1m_copy_raw_to_conformed.py:10-21,23-35` | Creates matching objects in `conformed/heartbeat/` if key absent | Validation should distinguish raw heartbeat generation from downstream promotion; page health alone does not prove conformed propagation |
| `dags/heartbeat_1m_copy_conformed_to_curated.py` | `heartbeat_1m_copy_conformed_to_curated` | Copies missing `heartbeat/` objects from `conformed` to `curated`. Evidence: `dags/heartbeat_1m_copy_conformed_to_curated.py:16-35` | scheduled | `*/1 * * * *` | Conformed heartbeat objects must exist; MinIO reachable at `http://minio:9000`. Explicit credentials are NOT VERIFIED in this DAG because boto3 client omits them. Evidence: `dags/heartbeat_1m_copy_conformed_to_curated.py:10-21,23-35` | Creates matching objects in `curated/heartbeat/` if key absent | Validation should include end-to-end heartbeat propagation if the checklist needs proof of all three buckets, not just Airflow scheduling |
| `dags/asx200_ohlcv_daily_to_raw.py` | `asx200_ohlcv_daily_to_raw` | Downloads recent ASX daily OHLCV from Yahoo Finance and writes one raw CSV per `(ticker, trade_date)`. Evidence: `dags/asx200_ohlcv_daily_to_raw.py:100-171` | manual only | `None` | `config/asx200_tickers.csv` must exist and be non-empty; Yahoo Finance access is required by code but external network success is NOT VERIFIED from repo alone; MinIO reachable. Evidence: `dags/asx200_ohlcv_daily_to_raw.py:24-27,47-85,100-124`; `docker-compose.yaml:82`; `README.md:81-92` | Writes raw CSV objects under `raw/tabular/market_ohlcv_daily/exchange=ASX/trade_date=.../ticker=...csv` | Validation cannot treat absence of ASX data as platform failure; docs explicitly say ASX DAGs are manual and absence of ASX data is not failure. Evidence: `README.md:106-113`; `RUNBOOK.md:333-339` |
| `dags/asx200_ohlcv_raw_to_conformed_parquet.py` | `asx200_ohlcv_raw_to_conformed_parquet` | Converts raw CSV OHLCV objects to parquet, adds ingest metadata, and keeps conformed append-only. Evidence: `dags/asx200_ohlcv_raw_to_conformed_parquet.py:41-128` | manual only | `None` | Raw OHLCV CSVs under the expected prefix must already exist; MinIO reachable; `pyarrow`/`pandas` installed. Evidence: `dags/asx200_ohlcv_raw_to_conformed_parquet.py:37-45,84-128`; `docker/airflow/Dockerfile:1-3` | Writes parquet objects to matching keys in `conformed`, adding `ingest_ts`, `ingest_batch_id`, `source_object_key`, `row_hash`; skips existing outputs | Validation should check transformation output and append-only skip behavior, not just DAG run success |
| `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py` | `asx200_ohlcv_conformed_to_curated_snapshot_v2` | Reads conformed parquet by trade date, dedupes by business key, and writes curated `snapshot.parquet`, overwriting snapshots when conformed is newer. Evidence: `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:39-42,87-170` | manual only | `None` | Conformed parquet objects must already exist; MinIO reachable; `pyarrow`/`pandas` installed. Evidence: `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:35-42,61-78,87-170`; `docker/airflow/Dockerfile:1-3` | Writes or refreshes curated `snapshot.parquet` per trade date | Validation should confirm snapshot creation and that downstream notebooks expecting curated parquet have data to read |
| `dags/asx200_ohlcv_backfill_to_raw.py` | `asx200_ohlcv_backfill_to_raw` | Backfill variant of the daily OHLCV DAG: 5-year download window, resume state, and audit errors. Evidence: `dags/asx200_ohlcv_backfill_to_raw.py:184-192` | manual only | `None` | Executable code currently reads local `config/asx200_tickers.csv`; comments and unused helper also describe a bucket-based control file, creating an internal source conflict. Evidence: `dags/asx200_ohlcv_backfill_to_raw.py:32-40,75-108,111-146,184-200`; `docker-compose.yaml:82` | Writes raw CSV OHLCV objects, updates `state.json`, appends `errors.json` audit entries | Validation scope must decide whether backfill state/audit artefacts are in scope; this DAG adds state beyond the normal daily path |

- Document/source alignment for trigger model is currently consistent for the six active DAGs: docs say heartbeat DAGs run automatically and ASX DAGs are manual only, while source sets heartbeat schedules to minute cron and ASX DAG schedules to `None`. Evidence: `README.md:105-113`; `RUNBOOK.md:333-347`; `dags/heartbeat_1m_to_raw.py:26-37`; `dags/heartbeat_1m_copy_raw_to_conformed.py:43-55`; `dags/heartbeat_1m_copy_conformed_to_curated.py:38-49`; `dags/asx200_ohlcv_daily_to_raw.py:174-186`; `dags/asx200_ohlcv_raw_to_conformed_parquet.py:131-142`; `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:173-185`; `dags/asx200_ohlcv_backfill_to_raw.py:338-349`.

## E. Notebook Validation Inventory

| File path | Apparent purpose | Dependencies evidenced | Reads from | Writes to | Output paths | Pre-existing data required |
| --- | --- | --- | --- | --- | --- | --- |
| `notebooks/hello_world.ipynb` | Simple notebook smoke content; prints `Hello World!` | Python kernel only evidenced | NOT VERIFIED | NOT VERIFIED | NOT VERIFIED | No pre-existing data evidenced. Evidence: `notebooks/hello_world.ipynb:18` |
| `notebooks/read_heartbeat.ipynb` | Lists heartbeat objects and reads latest heartbeat file contents from MinIO raw bucket | Installs `minio`; connects to `minio:9000` with `minioadmin` / `minioadmin` | MinIO `raw` bucket, prefix `heartbeat/` or empty prefix | NOT VERIFIED | NOT VERIFIED | Yes: requires heartbeat objects to exist, otherwise raises `RuntimeError("No heartbeat files found in raw bucket")` | `notebooks/read_heartbeat.ipynb:27,54-65,94-116` |
| `notebooks/read_ohlcv_daily.ipynb` | Reads curated OHLCV parquet objects from MinIO and performs basic EDA / memory inspection | Installs `pandas` and `pyarrow`; uses `Minio`; default endpoint `minio:9000` with `minioadmin` / `minioadmin` | MinIO `curated` bucket under `tabular/market_ohlcv_daily/exchange=ASX/` | NOT VERIFIED | NOT VERIFIED | Yes: requires curated parquet objects already present under the prefix | `notebooks/read_ohlcv_daily.ipynb:24,43,342-360,369-379` |
| `notebooks/01_asx_eda.ipynb` | Loads parquet dataset from MinIO, then performs EDA and plotting on ASX data | Uses `numpy`, `pandas`, `matplotlib`, `minio`; Jupyter image installs `pandas pyarrow matplotlib minio duckdb` | MinIO by default, bucket `curated`, prefix `tabular/market_ohlcv_daily/exchange=ASX/`; note says `minio:9000` should work in Docker and `localhost:9000` may be needed locally | NOT VERIFIED | NOT VERIFIED | Yes: default loader returns empty dataframe if no parquet objects found; meaningful EDA requires existing curated parquet data | `notebooks/01_asx_eda.ipynb:74,100-109,123-149,175`; `docker/jupyter/Dockerfile:1-5` |
| `notebooks/02_asx_preprocessing.ipynb` | Loads parquet dataset from MinIO, normalizes dtypes, engineers features, and persists a parquet output locally | Uses `numpy`, `pandas`, `matplotlib`, `minio`; Jupyter image installs `pandas pyarrow matplotlib minio duckdb` | MinIO by default, bucket `curated`, prefix `tabular/market_ohlcv_daily/exchange=ASX/` | File system | Default output path `asx_ohlcv_preprocessed.parquet` unless `ASX_PREPROCESSED_OUT` overrides it | Yes: default loader returns empty dataframe if no parquet objects found; meaningful preprocessing requires existing curated parquet data | `notebooks/02_asx_preprocessing.ipynb:63-71,83-106,121,340-342`; `docker/jupyter/Dockerfile:1-5` |

- Notebook reads from local files instead of MinIO are NOT VERIFIED except for `02_asx_preprocessing.ipynb` local parquet write. Evidence: `notebooks/02_asx_preprocessing.ipynb:340-342`.
- Notebook writes to MinIO are NOT VERIFIED for all notebooks. Evidence: no notebook source in `notebooks/*.ipynb` shows `put_object` or MinIO write calls; read paths are evidenced instead in `notebooks/read_heartbeat.ipynb:54-65,94-116`, `notebooks/read_ohlcv_daily.ipynb:356-379`, `notebooks/01_asx_eda.ipynb:123-149`, and `notebooks/02_asx_preprocessing.ipynb:83-106,340-342`.

## F. Existing Validation Hooks

| File | Validation hook | Scope | Current or likely stale |
| --- | --- | --- | --- |
| `docker-compose.yaml` | Container healthcheck for MinIO via `curl -f http://localhost:9000/minio/health/live` | MinIO container liveness / readiness for Compose dependency handling | Current. Evidence: `docker-compose.yaml:16-20,26-28` |
| `docker-compose.yaml` | Container healthcheck for Airflow via `curl -fsS http://localhost:8080/health` | Airflow container HTTP health | Current. Evidence: `docker-compose.yaml:92-96` |
| `php/health.php` | In-container TCP and HTTP checks for Airflow, Jupyter, MinIO API, and MinIO Console using `fsockopen` and `curl` | Cross-container reachability and HTTP responsiveness from the PHP container | Current. Evidence: `php/health.php:4-48,62-105` |
| `php/index.php` | Utility-page link to `/health.php` and service URL directory page | Human-accessible service index and entry point to health page | Current. Evidence: `php/index.php:5-15,28-66` |
| `README.md` | Declares platform health criteria: services start, Airflow UI accessible, heartbeat DAGs running; declares heartbeat DAGs are the indicator of platform health | High-level operational validation criteria | Current with current DAG source. Evidence: `README.md:88-91,105-113` |
| `RUNBOOK.md` | `docker ps --format ...` used to verify clean state and container health; documents heartbeat DAGs as health signal | Operational checklist for teardown/startup validation | Partially stale: validation commands are current, but `You should see ... supporting services (scheduler, webserver, etc.)` does not match the current single `airflow` service definition in `docker-compose.yaml`. Evidence: `RUNBOOK.md:268-300,302-307`; `docker-compose.yaml:41-96` |
| `config/README.md` | Repeats platform health criteria and ASX config expectations | Operator-facing validation note around config-dependent DAG failure | Current. Evidence: `config/README.md:7-19` |
| `docs/internal/TODO.md` | Explicitly says there is no evidence of automated tests and suggests adding a validation script for `raw`, `conformed`, and `curated` objects | Meta-evidence about missing automated validation | Current as a gap note | `docs/internal/TODO.md:10-12,20-22` |

- NO AUTOMATED TESTS EVIDENCED. Supporting evidence: repo-wide file search did not return project `test` / `tests` / `spec` / `smoke` / `validate` artefacts (`rg --files | rg '(^|/)(test|tests|spec|specs)(/|$)|smoke|validate'` returned no project files), and the repo TODO explicitly states `No evidence of automated tests for DAG logic or data transformations.` Evidence: `docs/internal/TODO.md:12`.

## G. Reset / Clean Environment Evidence

- Implemented reset mechanism: `./stop-compose.sh` runs `docker compose down`; `./stop-compose.sh --volumes` runs `docker compose down -v`. Evidence: `stop-compose.sh:43-49`.
- Repo docs define normal stop as preserving `minio-data` and `airflow-db`, while full reset deletes those volumes but retains repository files and bind-mounted directories including `config/`, `dags/`, `notebooks/`, `php/`, `logs/`, and `plugins/`. Evidence: `README.md:56-79,119-128`; `RUNBOOK.md:120-185`.
- RUNBOOK-defined extra cleanup for a "true clean build" includes optional image removal (`docker image rm ...`), Docker prune commands, and host-side deletion of `logs`, `eda_output`, and `.ipynb_checkpoints`. Evidence: `RUNBOOK.md:209-257`.
- RUNBOOK also says to verify clean state with `docker ps --format ...` and expect no project containers running. Evidence: `RUNBOOK.md:268-275`.

Supported definitions of "clean environment" from repo evidence:

- Minimal clean stack state, clearly supported: `./stop-compose.sh --volumes` removes stack containers and Docker volumes, which clears MinIO object storage and Airflow metadata DB while leaving bind-mounted repo content in place. Evidence: `stop-compose.sh:43-45`; `README.md:70-79`; `RUNBOOK.md:143-157,193-205`.
- Stronger "true clean build", documented but only partly implemented by scripts: remove custom images, prune unused Docker artefacts, and delete host runtime directories. Evidence: `RUNBOOK.md:209-257`.

Clearly required by repo evidence:

- For clearing persisted Compose state, `./stop-compose.sh --volumes` is required. Evidence: `stop-compose.sh:43-45`; `README.md:70-79`; `RUNBOOK.md:137-157`.

Clearly optional by repo evidence:

- Image removal is marked `Optional but recommended for a true clean build`. Evidence: `RUNBOOK.md:209-225`.
- Docker prune commands are described as removing unused artefacts without affecting other active projects, but not as required for normal reset. Evidence: `RUNBOOK.md:229-237`.
- Host runtime directory cleanup is part of RUNBOOK "Clean Teardown (Reset to Zero)" guidance, not part of `stop-compose.sh`. Evidence: `RUNBOOK.md:241-257`; `stop-compose.sh:43-49`.

Ambiguous or missing:

- Whether deleting `eda_output` actually targets a repo-root path or a path under `./notebooks` is NOT VERIFIED. RUNBOOK names `notebooks/eda_output/` in the structure view but later cleanup command removes `eda_output` without a `notebooks/` prefix. Evidence: `RUNBOOK.md:99-103,247-256`.
- Whether a "clean environment" requires removing notebook-created local files such as `asx_ohlcv_preprocessed.parquet` is NOT VERIFIED; no reset script or doc names that file explicitly. Evidence: `notebooks/02_asx_preprocessing.ipynb:340-342`; `RUNBOOK.md:241-257`.
- RUNBOOK claims that following steps exactly yields `no hidden state persists`, but the same runbook also states bind-mounted directories and local config are retained across `--volumes`; that stronger guarantee is therefore NOT VERIFIED from repo evidence. Evidence: `RUNBOOK.md:154-157,174-179,205,241-257,390-394`.

## H. Conflicts and Stale Docs

- `README.md` says the PHP landing page references unsupported services such as Metabase and ClickHouse, but current `php/index.php` lists only Airflow, Jupyter, MinIO Console, MinIO S3 API, and PHP itself. Source is authoritative; the README note is stale. Evidence: `README.md:147`; `php/index.php:5-15`.
- `docs/architecture.md` documents an `airflow-user-init` service, but current compose has no such service; Airflow user creation happens inside the main `airflow` service command. Source is authoritative; `docs/architecture.md` is stale here. Evidence: `docs/architecture.md:8-9,56,62`; `docker-compose.yaml:41-96`.
- `docs/architecture.md` uses old DAG names such as `raw_heartbeat_1m`, `raw_market_ohlcv_daily_asx`, and `conformed_to_curated_ohlcv_daily_snapshot`, but current DAG ids are `heartbeat_1m_to_raw`, `asx200_ohlcv_daily_to_raw`, and `asx200_ohlcv_conformed_to_curated_snapshot_v2`. Source is authoritative; the architecture doc is stale here. Evidence: `docs/architecture.md:23-37`; `dags/heartbeat_1m_to_raw.py:26-37`; `dags/asx200_ohlcv_daily_to_raw.py:174-186`; `dags/asx200_ohlcv_conformed_to_curated_snapshot_v2.py:173-185`.
- `docs/architecture.md` says ASX ingest runs every 5 minutes and takes tickers from Airflow Variable `ASX_TICKERS`, but current `asx200_ohlcv_daily_to_raw` sets `schedule=None` and reads `config/asx200_tickers.csv`; the old `Variable.get(...)` path is commented out. Source is authoritative; the architecture doc is stale here. Evidence: `docs/architecture.md:28-31`; `dags/asx200_ohlcv_daily_to_raw.py:40-43,47-85,174-186`.
- `RUNBOOK.md` says validation should show containers for `airflow`, `minio`, `jupyter`, and `supporting services (scheduler, webserver, etc.)`, but current compose defines only one `airflow` container and no separate scheduler/webserver services. Source is authoritative; the wording is stale or overly generic. Evidence: `RUNBOOK.md:302-307`; `docker-compose.yaml:41-96`.
- `dags/asx200_ohlcv_backfill_to_raw.py` contains an internal conflict: top comments and `_read_asx200_tickers_from_bucket()` describe a bucket-driven control file at `s3://raw/tabular/market_ohlcv_daily/control/asx200_backfill/asx200_tickers.csv`, but the executed path calls `_read_asx200_tickers_from_config()` and therefore depends on local `config/asx200_tickers.csv`. Executable code is authoritative for current behavior. Evidence: `dags/asx200_ohlcv_backfill_to_raw.py:32-40,111-146,184-200`.

## I. Unknowns Blocking Task 1 Execution

- Exact validation requirement for authenticated login success vs unauthenticated page accessibility is only explicit for Airflow UI accessibility; MinIO Console, MinIO API, Jupyter, and PHP do not define login-success criteria in repo evidence. Evidence: `README.md:88-91,95-99`; `RUNBOOK.md:321-368`.
- Expected success criteria for ASX manual DAG validation beyond object existence are NOT VERIFIED; docs say only that ASX DAGs are manual and absence of ASX data is not platform failure. Evidence: `README.md:106-113`; `RUNBOOK.md:333-347`.
- Whether Stage 2 should include the backfill DAG is ambiguous because it adds extra state/audit surfaces and has conflicting inline guidance about ticker source. Evidence: `dags/asx200_ohlcv_backfill_to_raw.py:32-40,47-48,75-108,111-146,184-200`.
- Exact clean-environment baseline is incomplete for notebook outputs: repo evidence defines volume reset and recommends some host cleanup, but does not fully enumerate all notebook-generated local artefacts. Evidence: `RUNBOOK.md:241-257`; `notebooks/02_asx_preprocessing.ipynb:340-342`.
- Notebook outcome expectations are NOT VERIFIED; notebooks exist, but repo docs explicitly say notebook outputs and dependencies are not verified against the running stack. Evidence: `docs/internal/TODO.md:10-12`; `docs/reference/IMPLEMENTED_CAPABILITIES.md:24`; `docs/internal/PROJECT_CONTEXT.md:26`.
- Automated validation scripts are absent, so any Stage 2 execution will rely on manual checks or ad hoc commands unless the prompt specifies exact checks. Evidence: `docs/internal/TODO.md:12,22`; repo file search described in section F.

## J. Proposed Inputs Needed For Stage 2 Execution Prompt

- Confirm which auth surfaces require authenticated login validation versus simple reachability/page access, especially for MinIO Console and Jupyter; repo evidence only makes Airflow UI accessibility explicit. Evidence: `README.md:88-99`; `RUNBOOK.md:321-368`.
- Provide or confirm the required local file `config/asx200_tickers.csv` for any ASX DAG execution scope. Evidence: `README.md:21,81-92`; `config/README.md:3-19`; `dags/asx200_ohlcv_daily_to_raw.py:47-85`.
- Decide whether Stage 2 includes only heartbeat validation or also manual ASX DAG execution and notebook validation, since docs treat heartbeat as the core health signal and ASX data absence as non-failure. Evidence: `README.md:105-113`; `RUNBOOK.md:333-347`.
- Decide whether `asx200_ohlcv_backfill_to_raw` is in scope despite its extra state/audit surfaces and conflicting inline guidance about ticker source. Evidence: `dags/asx200_ohlcv_backfill_to_raw.py:32-40,47-48,75-108,111-146,184-200`.
- Confirm the required clean-environment level: volume reset only, or also image removal, Docker prune, and host runtime directory cleanup. Evidence: `stop-compose.sh:43-49`; `RUNBOOK.md:193-257`.
- Confirm whether notebook validation should include local-file outputs such as `asx_ohlcv_preprocessed.parquet` and cleanup of notebook artefacts such as `eda_output` / `.ipynb_checkpoints`. Evidence: `notebooks/02_asx_preprocessing.ipynb:340-342`; `RUNBOOK.md:99-103,241-257`.
